QUANDO SALVIAMO IN UN FILE UNA VARIABILE E LE ASSEGNAMO LA PATH DI UN FILE IL PERCORSO INCOMINCIA DA DOVE ABBIAMO LANCIATO L'ESEGUIBILE DI NODE.JS E NON DALLA CARTELLA CHE CONTIENE IL FILE IN CUI SALVIAMO LA VARIABILE

QUANDO IMPORTIAMO UNA ROTTA IN UN FILE AD ESEMPIO QUELLA DI UN DATABASE NON DOBBIAMO IMPORTARLA DALLA NOSTRA FILE IN CUI SIAMO PRESENTI MA DOBBIAMO UTILIZZARE COME RIFERIMENTO LA CARTELLA DALLA QUALE ABBIAMO LANCIATO L'ESEGUIBILE DI NODEJS


await:
- chiamata al file system (che noi stiamo usando come db)
- una connessione col db
- chiamata http (get, post ecc)

query: è un oggetto che contiene tutte le k-v che passiamo dopo il search?

.send({
    data: {},
    error: true,
    message: 'user not found'
})

express-session
header

{
    "3-1": {
      "idu": "3",
      "idt": "1",
      "completed": true
    }
}

let [a, b, ...params] = process.argv // decostructoring?
process.argv è un array: ['PathDellaNostraMacchina', 'PathDeiNostriFile', qualcosaChePassiamoNelTerminale, qualcosaChePassiamoNelTerminale, ...] -> contiene tutti i dati che passiamo quando runniamo il nostro scipts
process è un oggetto

DOMANDE:
- perchè in gitignore dopo node_modules devo mettere un /
- se il tuo progetto non utilizzasse alcuna libreria potresti benissimo non utilizzare i node_modules
- perchè mi si installano quando installo express -> perchè creo una dipendenza che necessita dei relativi
- perchè non mi si installano i node_modules quando faccio npm install -> perchè non ho instllato alcuna dipendenza
- perchè preferire aync await a Sync


ALTRE INFORMAZIONI UTILI:

- axios:
import axios from 'axios'

async function call() {
    const res = await axios.post('http://localhost:3000/case-cinematografiche', {
        name: "Universal Pictures",
        fondazione: 1990,
        stato: "Stati Uniti"
    })
    console.log(res.status)
}
call()

- import:
import film from './database/film.json' assert { type: 'json' }
const DB_PATH = './db/users.json'